# Full public URL of the service, used to construct shareable page URLs.
# In production, set this to your domain or local DNS name.
# Example: http://pushpage.homelab.local
APP_SERVER_URL=http://localhost:8080

# Host port that nginx binds to.
# Change this if port 8080 is already in use on your host.
NGINX_PORT=8080

# ── MCP server (optional) ────────────────────────────────────────────────────
# Required only when running with --profile mcp.
# PUSHPAGE_API_KEY must be a valid API key for the pushpage service.
# PUSHPAGE_URL is set automatically to the internal Docker service name —
# you do not need to change it.
PUSHPAGE_API_KEY=pp_your_key_here
